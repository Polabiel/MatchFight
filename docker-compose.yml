services:
  db:
    image: postgres:16-alpine
    container_name: matchfight-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: matchfight
    ports:
      - "55432:5432"
    volumes:
      - matchfight-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d matchfight"]
      interval: 5s
      timeout: 5s
      retries: 10
  db-test:
    image: postgres:16-alpine
    container_name: matchfight-db-test
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: matchfight_test
    ports:
      - "55433:5432"
    volumes:
      - matchfight-db-test-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d matchfight_test"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  matchfight-db-data:
  matchfight-db-test-data:
